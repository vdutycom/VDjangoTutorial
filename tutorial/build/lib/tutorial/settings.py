"""
Django settings for tutorial project.

Generated by 'django-admin startproject' using Django 3.0.

For more information on this file, see
https://docs.djangoproject.com/en/3.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/3.0/ref/settings/
"""

import os
from datetime import datetime,timedelta
# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/3.0/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'x=$+t7pd7bl6wg@jcljrj)-0(td2w)$%w%-p-4#c)*7*&r=6h&'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = False

ALLOWED_HOSTS = ['*'] #可以让所有ip访问


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'rest_framework',
    'snippets',
   
]
THEME = "bootstrap"
STATIC_ROOT = "/Users/yeluxing/Desktop/mydevelop/python/study/tutorial2/tutorial/webstatic"
#静态资源配置start
'''
这个配置好了，在调试状态才能访问静态资源
'''
STATICFILES_DIRS = [os.path.join(BASE_DIR,"tutorial","themes","static")]

# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/3.0/howto/static-files/

STATIC_URL = '/static/'
#静态资源配置end


'''
*********************扩展user配置star*************
会有如下错误：
The field admin.LogEntry.user was declared with a lazy reference to 'snippets.user', but app 'snippets' doesn't provide model 'user'.
The field snippets.Snippet.owner was declared with a lazy reference to 'snippets.user', but app 'snippets' doesn't provide model 'user'.
所以，如果之前是使用例如 AUTH_USER_MODEL = auth.user 的用户模型，并重新将其自定义为 AUTH_USER_MODEL = blog.user 请删掉 migrations 目录下的所有文件 以及数据库文件。
删除之后，重新进行数据库的迁移
$ python manage.py makemigrations myapp
$ python manage.py migrate
所以，建议如果要对系统用户进行扩展，最好在项目的最初进行！
'''
#AUTH_USER_MODEL = 'snippets.User' snippets为app名称，这里大小写无关
'''********************扩展配user配置end*******************'''

REST_FRAMEWORK = {
    'DEFAULT_PERMISSION_CLASSES': (
        'rest_framework.permissions.IsAuthenticated',
    ),
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_jwt.authentication.JSONWebTokenAuthentication',
        'rest_framework.authentication.SessionAuthentication',
        'rest_framework.authentication.BasicAuthentication',
    ),
}

JWT_AUTH = {
    'JWT_EXPIRATION_DELTA': timedelta(days=3),
}

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'tutorial.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        #注意这个“tutorial/templates/”路径的写法，可以把templates放在项目的根目录，就可以“templates/”了
        'DIRS': [os.path.join(BASE_DIR, 'tutorial/templates/')], 
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'tutorial.wsgi.application'
#/Users/yeluxing/Desktop/mydevelop/python/study/tutorial2/tutorial/tutorial/templates/snippets/snippets.html 
#/Users/yeluxing/Desktop/mydevelop/python/study/tutorial2/tutorial/tutorial/templates/snipptes
print("0000sdfasdfasdfasdfasdfasdf",os.path.join(BASE_DIR, 'tutorial/templates'))

# Database
# https://docs.djangoproject.com/en/3.0/ref/settings/#databases

# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.sqlite3',
#         'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
#     }
# }

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'tutorial',
        'USER': 'root',
        'PASSWORD': '123456',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}

# Password validation
# https://docs.djangoproject.com/en/3.0/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/3.0/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True



